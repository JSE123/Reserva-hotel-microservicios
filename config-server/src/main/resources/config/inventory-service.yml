server:
  port: 8085

spring:
  # datasource:
  #   url: jdbc:mariadb://localhost:3306/hotel_inventory_db?createDatabaseIfNotExist=true
  #   username: root
  #   password: 
  #   driver-class-name: org.mariadb.jdbc.Driver
  # jpa:
  #   properties:
  #     hibernate:
  #       # dialect: org.hibernate.dialect.MariaDBDialect
  #       format_sql: true
  #   hibernate:
  #     ddl-auto: update
  #   generate-ddl: false
  #   show-sql: true

  datasource:
    url: jdbc:postgresql://localhost:5432/hotel_inventory_db
    username: postgres
    password: 1234
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    hibernate:
      ddl-auto: update
    generate-ddl: false
    show-sql: true
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8082
logging:
  level:
    org:
      springframework:
        security: DEBUG